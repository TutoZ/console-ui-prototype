# 候选保存试跑验证报告

日期：2026-09-08。范围：独立入口 design-io.html，内存演示数据，无真实接口；刷新恢复初始状态。实现版本以 source-hashes.json 为准。

## 已完成

| 检查 | 结果与证据边界 |
|---|---|
| 真实组件/样式绑定 | AST 校验 6 个槽位通过，检查导入来源、实际 JSX 引用和 Token 放置；不等于视觉完全一致 |
| 定向类型检查 | npm run design-io:typecheck 通过 |
| 自动测试 | npm run design-io:test：7 条状态测试、4 条绑定检查测试，共 11 条通过 |
| 构建 | npm run design-io:build 通过，独立产物 /tmp/jxl-design-io-build |
| 正常保存 | 浏览器点击保存后显示 V2 尚未应用，当前上岗存档仍为 V1 |
| 编辑隔离 | 修改草稿后已保存候选内容不随输入改变 |
| 异常恢复 | 打开测试工具注入一次失败，错误可见，输入保留；重试后保存 V3，当前版本仍 V1 |
| 返回再进入 | 返回演示团队，再进入继续培训，当前会话草稿保留；这里未实现真实产品离开确认流程 |
| 空输入 | 空白内容不可提交，有填写要求提示 |
| 键盘 | 原生 disabled 切换曾使保存后焦点落到 BODY；修复后按钮保留焦点，以 aria-disabled 表达不可提交，处理函数拦截；再次回车不新增候选 |
| 两种宽度 | 开发版 1366×768、1024×768 均观察截图与文档宽度，无横向溢出；1366 下检查按钮计算色值与面板圆角 |
| 固定构建复核 | 独立预览 5274 实际点击后仍为候选 V2 / 当前 V1，截图查看布局和状态文字正常 |

浏览器证据来自本任务 CUA 实际操作、DOM 快照与内联截图。以上是人工/Agent 观察记录，未归档为独立录屏或图片文件；不得当作后台持久化、请求日志或机器证据清单完整性已通过。自动测试代码可在 src/design-io/trainingState.test.ts 与 scripts/check-design-io-bindings.test.mjs 复跑。

## 问题与回流

发现：键盘保存引起焦点丢失。关联 CRAFT-02 / CRAFT-08；归因实现层。修复：需要保留操作位置时使用 aria-disabled，配套处理函数的重复触发拦截，并验证焦点与重复回车。该经验写入 craft.md；没有修改业务成功标准。

共享开发服务器曾被其他页面语法错误遮罩影响，因此改用独立构建预览隔离环境，不修改其他页面。全仓 npm run lint 存在其他源文件的类型/缺失模块错误，本次未解决；不能宣称全仓通过。独立构建还有三处京东字体资源解析警告，保持运行期路径；本页使用的字体已随构建打包，不等于全站字体已核验。

最终计划校验曾检出 lib/platformTerminology.ts 源码漂移。核对 git diff 后确认仅追加 SKILL_CREATE_CHAT，原有培训词义与导出未改；更新 catalog 的来源哈希后重新执行计划检查。未修改共享词表。

## 未完成与判定

AU-S01–S05 相关保存行为有局部观察，尚未按每条用例要求归档完整机器证据。AU-S06 正式测试资格关联未实现。其余测试、应用、历史及 16 条组合用例未通过本次试跑验证；真实权限、服务端幂等、持久化与线上版本隔离未联调。

结论：首个隔离保存交互样例及真实组件引用已接通，可供产品经理试用范式。G1 仍需组合证据与评审，G2 真实业务、G3 另一需求迁移均未达到。没有以 11 条代码测试替代 22 条交互断言或设计师视觉评审。
