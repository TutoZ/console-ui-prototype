【PRD】基于AOP模板创建Skill V2.0
版本号
更新日期
更新人
修改内容
变更类型
备注
V1.0
2026-08-17
傅琬岚
初稿完成，完整需求文档输出
新增
定稿，评审版
一、背景说明1.1 产品概述AOP 构建器是数字员工工作台中用于创建 Skill（技能）的核心能力单元。它采用「左侧 AI 助手对话引导 + 右侧 AOP 结构化表单」的双边协作模式，把 SKILL.md（技能规范文档）的编写拆解为几大要素，用户通过「填空」而非「写代码」完成技能创建，并可直接挂载平台已接入的脚本与知识库。
【PRD】基于AOP模板创建Skill V1.0
1.2 现状（V1.0 已上线）V1.0 已具备以下能力：
双侧协作创建：AI 按 6 步引导用户定义要素并自动填表，用户也可直接编辑表单，支持双向同步与 dirty 状态管理。
平台资源打通：Step 1 可挂载企业已接入脚本、关联线上知识库；上传自定义文件按格式落入 scripts/ references/。
草稿与发布：草稿保存、退出选项、三段式校验（必填→唯一性→结构/资源/安全自动修复）后发布。
1.3 当前问题基于 V1.0 上线后的实际表现，梳理出 5 个待解决问题：
序号
问题
表现
解决优先级
1
质量不可控
生成的 SKILL.md 质量不可控，校验仅能检验基本的格式与安全性
P0
问题解决中，概述见1.5
2
表单过重
字段多、填写逻辑不够流畅，用户容易放弃
P1
3
模板单一、装不下知识
一套六步表单硬套所有技能；流程型贴合，知识/判断/创作型并不完全适配
P1
4
只能创建、不能优化
AOP 表单编译出 SKILL.md 即被丢弃，技能上线后只能退回常规修改方式，无法基于运行反馈闭环优化
P1
5
无复用、规模不经济
同类业务反复从头写，无法沉淀（本期不解决）
P2
1.4 演进思路此前产品主线已明确：先打基础让用户用起来、收集反馈验证价值，再逐步解决当前问题：
让技能能被测试、被优化
让表单不那么重、能装下不同知识形态
让用户对生成物看得见、改得动，且发布后仍用同一套 AOP 交互持续改
1.5 sprint7迭代中需求（一）【工作台】AOP 对话创建技能二期：支持上传其他类型文件，作为创建技能的参考
与 V1 上传文件功能合并。
类型与处理方式：
类型
示例
处理方式
说明
沿用一期逻辑
.py .js .sh .ps1 .md
读取内容，按扩展名落盘（scripts/ 或 references/），可引用
既有能力
需 AI 理解（本期重点）
① .docx .pdf .xlsx .csv .txt ② png、jpg 等图片 ③ zip 等压缩包
上传 → 解析 → AI 抽取有用信息 → 作为填写表单或创建技能的参考；不直接放在对应目录中
不落盘为资源，仅作参考
增加的文件类型可逐步增加：① 常用文档类型；② 图片类型；③ 压缩包；④ 其他。
创建技能时文件上传支持多选：上传时可多选、支持一次性拖拽多个文件；多选最大文件个数上限 20 个，失败文件可展示简单失败原因，格式范围内的可单独重试。
（二）【非功能需求】优化创建相关提示词
效果目标：稳定（表单填写、技能生成）；生成技能（实验融合 skill-creator）。
二、需求目标本期聚焦解决 #1（只能创建不能优化）、#2（表单过重）、#3（模板单一），并对 #4（质量不可控）做重点改善尝试。目标可概括为三点：
技能可测试、可优化：提供自动/半自动测试与优化建议闭环，先在 AOP 创建场景落地，后续通用到全平台技能。
表单更轻、能装下不同知识形态：减负、按知识/动作自由拼装、长表单渐进披露。
生成物看得见、改得动：对话框 + 配置区（技能要素配置 / 文档编辑器 / 技能测试）两栏编辑面，发布后仍可用，持续优化不脱离 AOP 交互。
三、方案概述序号
当前问题
解决方案
对应需求
本期解决？
1
只能创建、不能优化
测试能力（用例管理+沙箱执行）+ 优化建议 + 发布后仍可编辑
3、4、5
✅ 解决
2
表单过重
长表单 + 渐进披露 + 分段折叠 + 平滑滚动 + 校验高亮跳转
2
✅ 解决
3
模板单一、装不下知识
混合类型表单，根据需求自由组装动作与知识
2
✅ 解决
4
质量不可控
四层测试 + 字段级定位 + 优化建议闭环
2、4
重点改善（本期重点投入，多维度尝试）
5
无复用、规模不经济
同类业务沉淀（本期不解决，后续规划）
—
❌ 本期不解决（P2）
表单减负与类型化
长表单按语义分段、可折叠、平滑滚动；将过去硬填入的字段拆分到更合理的维度。
测试与优化闭环
先提供「用例管理 → 沙箱执行 → 优化建议 → 采纳落地」的闭环，且在 AOP 创建场景首发，后续通用化到全平台技能。失败定位对 AOP 技能到字段级，其他来源技能降级到章节级。
发布后持续编辑
创建期与发布后共用同一编辑面，发布后从已保存 SKILL.md 重解析恢复表单/文档/对话，使"上线即终点"变为"上线即起点"。
四、需求范围数字员工工作台。
功能列表编号
功能名称
功能描述
优先级
1
构建工作台优化
页面含「对话框（AI 助手）+ 配置区（表单/测试用例/待扩充）+ 文档/结构视图（SKILL.md 全文 + 结构树）」
P0
2
表单优化
表单展示形式变为长表单
P1
3
测试用例管理
支持用户手动输入、批量上传、AI自动生成，可管理已有用例
P1
4
测试执行与优化建议
在数字员工沙箱逐条运行测试用例，获取运行轨迹与执行结果
P1
5
发布后编辑
通过AOP对话式创建方式发布的技能，编辑后仍进入对话-配置-编辑页面，支持创建时的各种交互操作
P2
五、需求详情1、构建工作台优化主流程：用户点开创建技能后，技能创建工作台左侧为对话框（AI助手），右侧配置区可切换“技能要素配置”、“文档编辑器”、“技能测试”
技能要素配置（见需求2）
文档编辑器：展示已生成的技能包结构和具体文档内容，可调整技能包结构（移动、删除），可编辑文档内容
技能测试（见需求3、4）
创建技能时，没有填完表单必填时不生成技能，编辑器页面提示需先完成表单必填项

修改表单和文档编辑器内容时，共享撤回和保存；AI 同时感知两侧改动并统一决策（处理后决定如何改表单/文档，可向用户澄清后再改）
其他功能优化：优化项
现状（截图/描述）
本期优化后
对话交互
消息发送后，模型以小碎嘴模式输出思考过程，未锁定表单内容
小碎嘴刷新
最终输出
小碎嘴内容优化，思考过程与最终输出展示上有明显区分；
思考中显示更明显：思考中表单/编辑框置灰不可编辑；
可点按钮停止思考；
可补充发新消息至消息队列
版本说明自动生成
发布新版本时，手动输入版本描述
所有技能发布更新时，可由 AI 根据 diff 自动生成版本描述，支持用户手动修改
2、表单优化（1）表单字段优化合并：收拢同源与相似字段，降低分辨与填写门槛；在此基础上精简表单数量。
拆分：把硬塞的字段拆成更合理的粒度，让缺失的“知识”独立成为可维护的字段。
“执行步骤”更新为“技能主体”，包含技能所需知识和具体执行步骤；用户自由增删「知识模块」与「步骤模块」，至少填一项（纯知识只填知识内容，纯步骤只填步骤说明，混合两者都填）；步骤中所需知识单独拆解。
技能定义
技能主体
规范约束
补充说明
表单
字段
页面展示说明
预填示例
必填
条件 / 限制
1 技能定义
技能名称
中文名
京东延保进度查询助手
必填
≤30 字，唯一
1 技能定义
技能标识
英文/拼音标识，系统内部引用；可手动填或留空自动从中文名生成
yb_progress
必填
小写字母/数字/下划线
1 技能定义
能力简介
这个技能能做什么（一句话能力概述）
查询京东延保服务单状态与处理进度
必填
≤50 字
1 技能定义
触发条件
什么情况应启用（用户问法/场景特征）
用户问延保进度时
必填
≤50 字
1 技能定义
禁止触发场景
什么情况不启用（路由避让）
非京东延保、非本人订单
选填
管"是否接活"
1 技能定义
用户输入信息
用户会提供的关键参数（讲"有什么"，非格式）
服务单号、手机号后四位
必填
与 3 规范约束·输入格式分工
1 技能定义
产出物
技能交付什么（讲"是什么"）
进度节点、处理方、预计完成时间
必填
与 3 规范约束·红线分工
1 技能定义
资源挂载·企业接入脚本
挂载要调的可执行脚本（用脚本时必填）
create_replace 脚本
选填
多选；须先挂后引
1 技能定义
资源挂载·企业知识库
挂载要召回的线上知识库（KB 召回时必填）
延保条款库
选填
多选
1 技能定义
资源挂载·其他技能包文件
挂载可引用的 references 文件（引用文件时必填；含本技能与其他技能包）
claims_process.md
选填
多选
2 技能主体
知识内容
一条模型需懂的背景知识（术语/概念/状态枚举/速查表）；可多条，全局常驻始终注入；支持挂载 KB/文件作为来源
服务单状态：审核中/维修中/换新中/已完成
知识/混合型必填
纯速查不检索，挂载来源的可检索
2 技能主体
知识说明
该知识的边界/适用范围/备注（原"知识来源/边界/适用范围"三字段已收敛为"内容+说明"）
仅京东延保订单适用
选填
—
2 技能主体
步骤名称
这一步的短名（AI 可从说明自动建议，可选填）
校验订单号
选填
—
2 技能主体
步骤说明
这步做什么（动作 / 知识都写这里）；支持挂载工具或资源、KB/文件来源；可任意增/插/拖拽排序
校验订单号并确认用户身份
流程/混合型必填
纯动作只填本项，纯知识可留空转填下项
2 技能主体
步骤知识
这步依赖的关键规则 / 判断 / 边界（即原"判断分支"）；支持挂载知识来源；含分支时写"条件→跳步骤 N"
不在保→跳兜底步骤
选填（含判断必填）
下拉选步骤序号，不画连线
2 技能主体
步骤示例
这步的输入→输出样例；运营写人话+样例，模型拿到更稳，且天然是 L3 golden 测试 fixtures
输入"单号123"→输出"已校验，在保"
选填（带工具/含判断推荐填）
—
3 规范约束
禁止行为
绝对不能做
不得编造物流状态、不得泄露他人订单
选填
恒显
3 规范约束
内容红线
必须含/禁止含的内容要求
必须含处理时效；禁止承诺具体赔付
选填
恒显
3 规范约束
回答口径
引用规则、话术范式
必须引用售后条款库
选填
知识型建议填
3 规范约束
兜底策略
答不了/低置信时怎么办（AI 引导时会主动关注并鼓励填写，降低"答不上来乱编"风险）
不确定则转人工核实
选填
恒显
3 规范约束
表达风格
语气/长度上限/禁用词
温和安抚、≤80 字、禁用"亲"
选填
恒显
4 补充说明
示例
场景示例：用户问法+期望回复+注意点；也可填典型 I/O；运营在文档里展开
用户「单子三天没动静」→展示进度+转人工
选填
推荐填，助生成+测试
4 补充说明
补充资料
模板装不下的长内容/备注（业务速查表、召回触发规则、输出规范等）
业务知识速查表 §1–§6
选填
专家/AI 填，进文档

（2）表单交互优化优化项
现状（截图/描述）
本期优化后
表单样式
表单以单页长表单呈现，每段可折叠收纳
表头有横向导航
资源挂载
知识库在先，增加上传知识库按钮，跳转至知识库上传页面，保存草稿；
企业接入脚本在后，若无脚本则不展示
填写过程
每个字段都会询问，先问了再填写
单个表单填写完后跳转到下一个表单
可一次性生成多个字段（优先生成必填项），可跨表单填写，先填写再问
AI填写完后不自动跳转页面位置，用户自由审阅生成的内容，减少用户手动完整输入项
填写完成提示
填写完成不明显
段落必填项完成后页面轻微提示，每个字段填写内容后页面上增加✅️提示
校验发布
必填项未填时表单对应位置高亮提示，页面直接跳转到第一个未填的必填项；
名称重复时，表单对应位置高亮提示，页面跳转到该处
3、测试用例管理主流程：支持通过三种方式新建测试用例，每个测试用例包括输入和期望（非必填，可以是输出、可以是执行过程）
手动输入：选择手动输入，允许仅给输入、不提供期望结果（无期望由 AI 基于 SKILL.md 与执行合理性判定是否正确）；带期望的转为「输入 + 期望判定」回归用例
文件上传：上传测试集 / 导入历史 bad case；允许仅给输入、不提供期望结果（无期望由 AI 基于 SKILL.md 与执行合理性判定是否正确）；带期望的转为「输入 + 期望判定」回归用例
AI 自动生成：点「AI自动生成」，系统读 SKILL.md 触发条件 / 执行流程 / 边界案例，用户可输入生成条数，默认5个，每个case包含输入和期望结果；生成的用例进入用例管理列表，与手动/上传用例一样可查看、编辑、删除。
管理已有：测试集视图表格列出全部用例，一页30条，支持翻页，每个case 可查看 / 编辑 / 删除输入与期望结果。
4、测试执行与优化建议主流程：在测试用例管理页面，勾选测试用例，点击运行测试
系统将选定的用例送入数字员工沙箱逐条运行被测技能，获取运行轨迹与执行结果，包括：
触发路由（该触发时触发、不该触发时避让、多技能冲突）
行为场景（执行过程是否符合要求）
工具脚本（脚本是否被正确调用）
效果质量（最终的输出是否达到预期）
测试执行完成后，左侧对话窗口展示结果总览（通过率、pass/fail/不确定分布），逐条可查输入 / 实际产出 / 期望 / 差异点
结果判定与失败定位：系统基于期望结果和自己的推理，比对实际产出，判定测试是否通过；对于无期望结果的用例，系统基于 SKILL.md 内容与执行合理性自行推理判定是否正确；失败按类型与层聚类；定位失败原因：AOP 技能定位到字段级，其他来源技能降级章节级
生成优化建议与采纳：AI基于失败聚类生成改进建议，对话中展示测试结果与优化建议，询问用户是否采纳，用户确认采纳后方可执行修改
5、发布后编辑主流程：【员工技能】-【团队技能】中，已发布且为AI创建的技能，点击【编辑】
进入技能构建工作台，原AI对话记录、表单内容、SKILL.md、测试用例等全部恢复，用户看到与创建期一致的交互，复用已有功能
优化仅更新文档草稿，不自动发布；点击发布，校验成功后创建新版本。
异常处理：若出现字段缺失或解析失败，提示用户：可选择继续编辑（手动补充缺失字段），或切换至普通文档编辑器（无构建工作台）直接修改 SKILL.md。